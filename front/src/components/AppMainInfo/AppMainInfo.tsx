import styles from './AppMainInfo.module.css';

export const AppMainInfo = () => {
    return (
        <div className={styles.titleContainer}>
            <h1 className={styles.mainHeader}>You need some advice?</h1>
        </div>
    );
};
